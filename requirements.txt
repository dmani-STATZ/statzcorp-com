# ── Core ──────────────────────────────────────────────────
Django==5.2.*
whitenoise[brotli]==6.9.*     # Serves static files; no CDN needed in GCCH
python-decouple==3.8.*        # Pulls settings from .env / Azure App Settings
django-storages[azure]>=1.14  # Azure Blob Storage for video/media uploads (GCCH)

# ── Database ──────────────────────────────────────────────
# Current: SQLite (built into Python / Django — no extra package).
# Future (MSSQL): uncomment when migrating to Microsoft SQL Server.
# Attempted 2026-07-14 and reverted — do not re-enable until BOTH blockers
# in statzcorp/settings/production.py are resolved (ODBC Driver 18
# persistence on App Service Linux + VNet Integration to private DB host):
# mssql-django==1.*
# pyodbc==5.*

# ── File handling ─────────────────────────────────────────
Pillow==11.*                  # Image field support

# ── Azure & Auth (add when ready for internal views) ──────
# msal==1.31.*                # Microsoft Authentication Library
# django-microsoft-identity-web  # Entra ID SSO for GCCH

# ── Production WSGI server ────────────────────────────────
gunicorn==23.*                # Used by Azure App Service startup command
